bind = $mainMod Shift, Space, togglefloating,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, F, fullscreen

# Move focus with mainMod + arrow keys
bindt = $mainMod, left, movefocus, l
bindt = $mainMod, right, movefocus, r
bindt = $mainMod, up, movefocus, u
bindt = $mainMod, down, movefocus, d

# Move Windows Around
bindt = $mainMod SHIFT, left, movewindow, l
bindt = $mainMod SHIFT, right, movewindow, r
bindt = $mainMod SHIFT, up, movewindow, u
bindt = $mainMod SHIFT, down, movewindow, d

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Select Windows with Alt+Tab
bind = ALT, Tab, cyclenext,
bind = ALT, Tab, bringactivetotop

# Open hyprexpo
# bind = $mainMod, space, hyprexpo:expo, toggle
